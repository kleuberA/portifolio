import Menu from "../menu";
import ContainerHero from "./Container";

export default function Hero() {
    return (
        <section className="w-full min-h-screen">
            <Menu />
            <ContainerHero />
        </section>
    )
}